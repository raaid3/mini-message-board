<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add message</title>
  </head>
  <body style="font-family: Arial, sans-serif; margin: 2rem">
    <form
      method="post"
      action="/new"
      style="
        max-width: 400px;
        margin: auto;
        display: flex;
        flex-direction: column;
        gap: 1rem;
        background-color: #f9f9f9;
        border: 1px solid #ddd;
        padding: 1rem;
        border-radius: 5px;
      "
    >
      <h1 style="text-align: center">Type in your message</h1>

      <label for="userName" style="font-weight: bold">Your Name:</label>
      <input
        type="text"
        name="userName"
        id="userName"
        style="padding: 0.5rem; border: 1px solid #ccc; border-radius: 4px"
        required
      />

      <label for="messageText" style="font-weight: bold">Your Message:</label>
      <textarea
        id="messageText"
        name="messageText"
        rows="4"
        style="padding: 0.5rem; border: 1px solid #ccc; border-radius: 4px"
        required
      ></textarea>

      <button
        type="submit"
        style="
          background-color: #333;
          color: #fff;
          padding: 0.75rem;
          border: none;
          border-radius: 4px;
          font-weight: bold;
          cursor: pointer;
        "
      >
        Submit
      </button>
    </form>
  </body>
</html>
